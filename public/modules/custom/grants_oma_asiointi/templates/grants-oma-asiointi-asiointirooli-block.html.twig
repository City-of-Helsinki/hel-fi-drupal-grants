{% set name = title ? title : 'Private person'|t %}
{% set link = title ? '' : switchRole %}

<div class="asiointirooli-block">
  <div class="container">
    <span class="asiointirooli--rooli">{{ "Asiointirooli"|t }}: {{ name }}</span> {{ link }}
  </div>
</div>
