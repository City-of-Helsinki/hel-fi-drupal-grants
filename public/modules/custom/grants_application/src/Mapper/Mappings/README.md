# Naming

Under Mappings-folder, set the application_type_id folder
Under application_type_id folder, the actual mapping-json file is named after the form_identifier.

Form identifiers can be found from form_types.json -file.
