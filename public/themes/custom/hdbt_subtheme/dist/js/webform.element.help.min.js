
(() => {
  var __defProp = Object.defineProperty;
  var __name = (target, value) => __defProp(target, "name", { value, configurable: true });

  // src/js/webform.element.help.js
  (($, Drupal, once) => {
    Drupal.webform = Drupal.webform || {};
    Drupal.webform.elementHelpIcon = Drupal.webform.elementHelpIcon || {};
    Drupal.webform.elementHelpIcon.options = Drupal.webform.elementHelpIcon.options || {};
    Drupal.behaviors.webformElementHelpIcon = {
      attach(context) {
        const { tippy } = window;
        if (!tippy) {
          return;
        }
        const hideOnEsc = {
          name: "hideOnEsc",
          defaultValue: true,
          fn: /* @__PURE__ */ __name(function fn(_ref) {
            const { hide } = _ref;
            function onKeyDown(event) {
              if (event.keyCode === 27) {
                hide();
              }
            }
            __name(onKeyDown, "onKeyDown");
            return {
              onShow: /* @__PURE__ */ __name(function onShow() {
                document.addEventListener("keydown", onKeyDown);
              }, "onShow"),
              onHide: /* @__PURE__ */ __name(function onHide() {
                document.removeEventListener("keydown", onKeyDown);
              }, "onHide")
            };
          }, "fn")
        };
        $(once("webform-element-help", ".js-webform-element-help", context)).each(/* @__PURE__ */ __name(function fn() {
          const $link = $(this);
          $link.on("click", (event) => {
            event.preventDefault();
          });
          const options = $.extend(
            {
              content: $link.attr("data-webform-help"),
              delay: 100,
              allowHTML: true,
              interactive: false,
              trigger: "click",
              plugins: [hideOnEsc]
            },
            Drupal.webform.elementHelpIcon.options
          );
          tippy(this, options);
        }, "fn"));
      }
    };
  })(jQuery, Drupal, once);
})();
//# sourceMappingURL=webform.element.help.min.js.map
