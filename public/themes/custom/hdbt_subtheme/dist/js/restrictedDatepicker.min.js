
(() => {
  // src/js/restrictedDatepicker.js
  ((Drupal) => {
    Drupal.behaviors.restrictedDatepicker = {
      attach(_context, settings) {
        const applicationTypeDateFieldMap = {
          LIIKUNTATAPAHTUMA: { start_date_field: "edit-alkaa", end_date_field: "edit-paattyy" },
          NUORPROJ: { start_date_field: "edit-projekti-alkaa", end_date_field: "edit-projekti-loppuu" },
          KASKOIPLISA: { start_date_field: "edit-alkaen", end_date_field: "edit-paattyy" },
          KUVAPROJ: { start_date_field: "edit-hanke-alkaa", end_date_field: "edit-hanke-loppuu" },
          KUVAKEHA: { start_date_field: "edit-hanke-alkaa", end_date_field: "edit-hanke-loppuu" }
        };
        if (!settings.restricted_datepicker || !settings.restricted_datepicker.application_type) {
          return;
        }
        const applicationType = settings.restricted_datepicker.application_type;
        const startDateInput = document.getElementById(applicationTypeDateFieldMap[applicationType].start_date_field);
        const endDateInput = document.getElementById(applicationTypeDateFieldMap[applicationType].end_date_field);
        if (startDateInput && endDateInput) {
          startDateInput.addEventListener("change", () => {
            endDateInput.min = startDateInput.value;
            if (endDateInput.value && startDateInput.value > endDateInput.value) {
              endDateInput.value = startDateInput.value;
            }
          });
        }
      }
    };
  })(Drupal);
})();
//# sourceMappingURL=restrictedDatepicker.min.js.map
